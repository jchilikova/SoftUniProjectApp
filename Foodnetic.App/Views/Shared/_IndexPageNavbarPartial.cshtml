@using Foodnetic.Contants
@using Foodnetic.Models
@using Microsoft.AspNetCore.Identity

@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager

@if (SignInManager.IsSignedIn(User))
{
    if (User.IsInRole(Constants.Strings.AdministratorRole))
    {
        <nav class="navbar navbar-default navbar-fixed-top">
            <div class="container">
                <!-- Brand and toggle get grouped for better mobile display -->
                <div class="navbar-header page-scroll">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand page-scroll" href="#page-top"><img src="~/images/logo2.png" alt="Foodnetic theme logo" width="250" height="50"></a>
                </div>
                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav navbar-right">
                        <li>
                            <a asp-action="Index" asp-controller="Home">Home</a>
                        </li>
                        <li>
                            <a asp-action="All" asp-controller="Recipes">Recipes</a>
                        </li>
                        <li>
                            <a asp-action="Index" asp-controller="Menus">Menu</a>
                        </li>
                        <li>
                            <a asp-action="Index" asp-controller="Fridge">Virtual Fridge</a>
                        </li>
                        <li class="nav-item">
                            <a asp-area="Administration" asp-controller="Admins" asp-action="CreateProduct" class="nav-link">Add Product</a>
                        </li>
                        <li>
                            <a asp-action="Logout" asp-controller="Account">Logout</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    }
    else
    {
        <nav class="navbar navbar-default navbar-fixed-top">
            <div class="container">
                <!-- Brand and toggle get grouped for better mobile display -->
                <div class="navbar-header page-scroll">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand page-scroll" href="#page-top"><img src="~/images/logo2.png" alt="Foodnetic theme logo" width="250" height="50"></a>
                </div>
                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav navbar-right">
                        <li>
                            <a asp-action="Index" asp-controller="Home">Home</a>
                        </li>
                        <li>
                            <a asp-action="All" asp-controller="Recipes">Recipes</a>
                        </li>
                        <li>
                            <a asp-action="Index" asp-controller="Menus">Menu</a>
                        </li>
                        <li>
                            <a asp-action="Index" asp-controller="Fridge">Virtual Fridge</a>
                        </li>
                        <li>
                            <a asp-action="Logout" asp-controller="Account">Logout</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    }
}
else
{
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header page-scroll">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand page-scroll" href="#page-top"><img src="~/images/logo2.png" alt="Foodnetic theme logo" width="250" height="50"></a>
            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-right">
                    <li class="hidden">
                        <a href="#page-top"></a>
                    </li>
                    <li>
                        <a asp-action="Register" asp-controller="Account">Register</a>
                    </li>
                    <li>
                        <a asp-action="Login" asp-controller="Account">Login</a>
                    </li>
                    <li>
                        <a class="page-scroll" href="#contact">Contact</a>
                    </li>
                    <li>
                        <a class="page-scroll" href="#about">About</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
}

